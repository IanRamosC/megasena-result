{
  "name": "megasena-result",
  "version": "0.0.7",
  "description": "A module to generate random number to help you win the lottery.",
  "main": "./megasena.js",
  "preferGlobal": true,
  "bin": {
    "megasena-result": "./bin/megasena"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha --compilers js:babel-core/register --reporter spec",
    "compile": "./node_modules/.bin/babel src/megasena.js --out-file ./megasena.js",
    "build": "npm test && npm run compile"
  },
  "keywords": [
    "megasena",
    "lottery",
    "random",
    "numbers"
  ],
  "author": "https://github.com/ianramosc",
  "repository": "https://github.com/ianramosc/megasena-result",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "chai": "^3.5.0",
    "mocha": "^3.1.2"
  }
}
