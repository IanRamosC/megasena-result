#!/usr/bin/env node
'use strict';

var num
  , megasena = require('../megasena.js')


num = (process.argv[2] && !isNaN(process.argv[2])) ? Number(process.argv[2]) : 6;
console.log(megasena(num, true))

